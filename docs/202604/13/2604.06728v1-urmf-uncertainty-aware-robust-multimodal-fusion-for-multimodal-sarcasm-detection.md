---
title: "URMF: Uncertainty-aware Robust Multimodal Fusion for Multimodal Sarcasm Detection"
title_zh: URMF：一种考虑不确定性的稳健多模态融合方法用于多模态讽刺检测
authors: "Zhenyu Wang, Weichen Cheng, Weijia Li, Junjie Mou, Zongyou Zhao, Guoying Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06728v1"
tags: ["query:q2"]
score: 9.0
evidence: 基于文本与图像语义不一致的多模态讽刺检测
tldr: 本文针对多模态讽刺检测中不同模态可靠性不均的问题，提出URMF模型，通过显式建模模态不确定性，利用自注意力和跨模态交互机制，并结合不确定性驱动的对比学习，有效增强模型对噪声模态的鲁棒性，提高检测准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06728v1/fig-001.webp\", \"caption\": \"Figure 2: t-SNE visualization of joint latent representations on the MSD test set for major ablation variants and the full URMF.\", \"page\": 9, \"index\": 1, \"width\": 976, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.06728v1/fig-002.webp\", \"caption\": \"Table 2: Main results on the MSD dataset. Best results are shown in bold and second-best results are underlined.\", \"page\": 8, \"index\": 2, \"width\": 772, \"height\": 1029}, {\"url\": \"assets/figures/arxiv/2604.06728v1/fig-003.webp\", \"caption\": \"Figure 1: Given an image-text pair (I, T ), URMF performs cross-modal interaction, modality uncertainty modeling, and uncertainty-guided dynamic fusion to learn a joint latent representation for sarcasm prediction. The model is trained end-to-end with information bottleneck, modality regularization, multimodal alignment, and uncertainty contrastive learning losses.\", \"page\": 4, \"index\": 3, \"width\": 968, \"height\": 537}]"
motivation: 现有方法假设各模态可靠性相同，但现实中常有不相关或噪声模态，导致融合不稳。
method: 采用多头跨注意力与自注意力结合的交互机制，并通过高斯分布建模模态不确定性来动态调节模态贡献。
result: 在多个公开多模态讽刺检测基准上，URMF均优于强劲的单模态、多模态及大模型基线。
conclusion: URMF通过引入不确定性感知融合机制显著提升了多模态讽刺检测的准确性与鲁棒性。
---

## 摘要
多模态讽刺检测（Multimodal Sarcasm Detection, MSD）的目标是通过识别文本与图像之间的语义不一致来判断讽刺意图。尽管近期方法通过跨模态交互和不一致性推理提升了MSD的性能，但它们通常假设各模态同等可靠。然而，在真实的社交媒体环境中，文本内容可能存在歧义，而视觉内容可能仅弱相关甚至不相关，这会导致确定性融合引入噪声证据，从而削弱稳健的推理能力。为解决这一问题，我们提出了Uncertainty-aware Robust Multimodal Fusion（URMF），一个在交互和融合过程中显式建模模态可靠性的统一框架。URMF首先采用多头跨注意力机制（multi-head cross-attention）将视觉证据注入到文本表示中，随后在融合后的语义空间中使用多头自注意力（multi-head self-attention）以增强不一致性感知推理。接着，它通过将每个模态参数化为可学习的高斯后验，对文本、图像及交互感知潜表示进行统一的单模态偶然不确定性（aleatoric uncertainty）建模。估计得到的不确定性进一步用于在融合过程中动态调节各模态的贡献，从而抑制不可靠的模态，获得更加稳健的联合表示。此外，我们设计了一个联合训练目标，结合了任务监督、模态先验正则化、跨模态分布对齐以及基于不确定性的自采样对比学习。基于公共MSD基准数据集的实验结果表明，URMF在单模态、多模态及基于MLLM的强基线模型上均取得一致优于的性能，充分验证了不确定性感知融合在提升准确性与鲁棒性方面的有效性。

## Abstract
Multimodal sarcasm detection (MSD) aims to identify sarcastic intent from semantic incongruity between text and image. Although recent methods have improved MSD through cross-modal interaction and incongruity reasoning, they often assume that all modalities are equally reliable. In real-world social media, however, textual content may be ambiguous and visual content may be weakly relevant or even irrelevant, causing deterministic fusion to introduce noisy evidence and weaken robust reasoning. To address this issue, we propose Uncertainty-aware Robust Multimodal Fusion (URMF), a unified framework that explicitly models modality reliability during interaction and fusion. URMF first employs multi-head cross-attention to inject visual evidence into textual representations, followed by multi-head self-attention in the fused semantic space to enhance incongruity-aware reasoning. It then performs unified unimodal aleatoric uncertainty modeling over text, image, and interaction-aware latent representations by parameterizing each modality as a learnable Gaussian posterior. The estimated uncertainty is further used to dynamically regulate modality contributions during fusion, suppressing unreliable modalities and yielding a more robust joint representation. In addition, we design a joint training objective integrating task supervision, modality prior regularization, cross-modal distribution alignment, and uncertainty-driven self-sampling contrastive learning. Experiments on public MSD benchmarks show that URMF consistently outperforms strong unimodal, multimodal, and MLLM-based baselines, demonstrating the effectiveness of uncertainty-aware fusion for improving both accuracy and robustness.

---

## 论文详细总结（自动生成）

# URMF：一种考虑不确定性的稳健多模态融合方法用于多模态讽刺检测 — 深度总结

---

## 一、研究背景与核心问题

- **任务定位**：多模态讽刺检测（Multimodal Sarcasm Detection, MSD）旨在识别文本与图像之间的语义不一致，以推断讽刺意图。
- **现有问题**：
  - 主流模型假设不同模态（文本、图像）**同等可靠**。
  - 真实社交媒体中：
    - 文本可能具有歧义、缺失上下文；
    - 图像可能仅作装饰或与讽刺语义**弱相关甚至不相关**。
  - 这种“确定性融合”会将噪声模态作为可靠证据，引入错误线索，削弱鲁棒性。
- **研究动机**：作者希望通过**不确定性建模**明确刻画各模态可靠性，动态抑制噪声模态，从而提升跨模态推理与鲁棒性。

---

## 二、方法概述与技术细节

### 1. 核心思想
提出 **URMF（Uncertainty-aware Robust Multimodal Fusion）** 框架，通过统一的跨模态交互与模态不确定性建模，在融合阶段自适应调节模态贡献。

### 2. 主体架构流程
1. **跨模态交互模块**  
   - 使用单层 Transformer，采用 *“先跨模态对齐，再自模态推理”* 的反向设计。  
   - 通过 **多头跨注意力（Multi-Head Cross-Attention, MHCA）** 将视觉证据注入文本序列，再使用 **多头自注意力（MHSA）** 捕捉融合语义空间中的令牌依赖。  
   - 最终得到交互感知的潜表示 \( X_f \)。

2. **单模态偶然不确定性（Aleatoric Uncertainty）建模**  
   - 对文本、图像以及交互感知模态分别建模。  
   - 每个模态的潜表示都视为高斯随机变量：  
     \[
     p(z_m | x_m) = \mathcal{N}(\mu_m, \sigma_m^2I)
     \]
     其中均值表示语义中心，方差描述模态不确定性；
   - 通过可学习的均值与方差头预测参数，并使用重参数化采样。

3. **不确定性引导的动态融合**  
   - 基于模态的平均方差计算整体不确定性；
   - 将不确定性经指数映射转化为权重：
     \[
     \alpha_m = \frac{\exp(1/(\bar{\sigma}_m^2 + \varepsilon))}{\sum\limits_{s\in\{f,i\}} \exp(1/(\bar{\sigma}_s^2+\varepsilon))}
     \]
   - 权重用于动态调节模态贡献，使低不确定性模态占更大比重；
   - 最终联合表示再经一层网络与高斯建模得到分类表示。

4. **联合优化目标**
   综合四类损失：
   - **信息瓶颈约束（LIB）**：通过交叉熵与 KL 散度，压缩表示但保持任务可辨性；
   - **模态正则化（Lreg）**：使各模态潜分布接近标准高斯，稳定训练流程；
   - **跨模态对齐（Lalign）**：在分布上对齐文本、图像与交互潜空间；
   - **不确定性驱动的对比学习（LUCL）**：利用采样噪声生成增强视图进行对比学习，提高表示鲁棒性。

---

## 三、实验设计与对比方案

- **数据集**：公开的 **MSD（Multimodal Sarcasm Detection）** 数据集。
- **评估指标**：Accuracy、Precision、Recall、F1-score。
- **对比基线**：
  - *单模态*：如 ResNet、ViT（图像），Bi-LSTM、BERT、RoBERTa（文本）；
  - *多模态*：HFM、DynRT-Net、MICL、MuMu、CIRM、SCI-GDFN 等；
  - *大模型基线*：LLaVA1.5、LLaVA1.5-VIDR。
- **结果指标（核心表现）**：
  - URMF 在 MSD 上达到 **95.02% Accuracy / 94.91% F1**，超越全部基线。

---

## 四、资源与算力情况

- **硬件使用**：单张 **NVIDIA GeForce RTX 4090 GPU**。
- **未提到**：训练时长与具体批次大小未在论文中明确说明。
- **结论**：模型训练资源需求中等，适合单卡实验。

---

## 五、实验数量与充分性

- **主要实验内容**：
  - 总体性能对比（表 2）；
  - 六个**消融实验**（表 3），分别去除四个子损失和动态融合机制、交互顺序；
  - **t-SNE 可视化** 展示潜空间分布变化。
- **充分性评价**：
  - 涵盖完整模型及各子模块验证；
  - 与强多模态、单模态及 MLLM 基线对比；
  - 实验设计全面、验证充分；
  - 但数据集仅限 MSD，尚未验证跨域泛化。

---

## 六、主要结论

- URMF 明确刻画模态可靠性与噪声水平；
- 不确定性驱动的动态融合显著提升**鲁棒性与精度**；
- 对比大模型（如 LLaVA 系列）仍保持领先，说明任务定制的不确定性建模更有效；
- 所有子模块均有正向贡献，去除任意部分性能下降 0.5–1.5个百分点。

---

## 七、优点与创新亮点

- **方法创新**：
  - 引入显式的单模态高斯不确定性建模；
  - 动态模态融合权重调节机制；
  - 将不确定性用于数据增强式对比学习。
- **架构层面**：
  - 跨模态交互顺序反转（先跨模态再自模态）更适合讽刺语义冲突建模；
  - 设计雅致且端到端可训练。
- **性能层面**：
  - 在多模态讽刺检测领域刷新 SOTA；
  - 在精度与鲁棒性指标上均衡表现优良。

---

## 八、不足与局限

- **数据覆盖有限**：仅在 MSD 一个公开数据集上评估，缺乏多领域泛化验证。
- **算力和效率未详述**：训练时间、模型参数量未公布。
- **理论局限**：
  - 不确定性仅限于偶然不确定性（aleatoric），未探讨认知层不确定性（epistemic）。
  - 动态融合依赖高斯分布假设，对复杂模态分布可能不够灵活。
- **应用受限**：
  - 针对讽刺检测任务，推广至其他多模态任务仍需验证。

---

（完）
