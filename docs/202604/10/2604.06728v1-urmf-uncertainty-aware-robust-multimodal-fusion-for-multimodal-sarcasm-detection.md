---
title: "URMF: Uncertainty-aware Robust Multimodal Fusion for Multimodal Sarcasm Detection"
title_zh: URMF：不确定性感知的鲁棒多模态融合用于多模态讽刺检测
authors: "Zhenyu Wang, Weichen Cheng, Weijia Li, Junjie Mou, Zongyou Zhao, Guoying Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06728v1"
tags: ["query:q2"]
score: 9.0
evidence: 基于文本与图像语义不一致性的多模态讽刺检测
tldr: 本文提出URMF框架，用于多模态讽刺检测，通过在文本与图像融合过程中显式建模模态不确定性，动态调整各模态贡献，提高跨模态推理的可靠性和检测准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06728v1/fig-001.webp\", \"caption\": \"Figure 2: t-SNE visualization of joint latent representations on the MSD test set for major ablation variants and the full URMF.\", \"page\": 9, \"index\": 1, \"width\": 976, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.06728v1/fig-002.webp\", \"caption\": \"Table 2: Main results on the MSD dataset. Best results are shown in bold and second-best results are underlined.\", \"page\": 8, \"index\": 2, \"width\": 772, \"height\": 1029}, {\"url\": \"assets/figures/arxiv/2604.06728v1/fig-003.webp\", \"caption\": \"Figure 1: Given an image-text pair (I, T ), URMF performs cross-modal interaction, modality uncertainty modeling, and uncertainty-guided dynamic fusion to learn a joint latent representation for sarcasm prediction. The model is trained end-to-end with information bottleneck, modality regularization, multimodal alignment, and uncertainty contrastive learning losses.\", \"page\": 4, \"index\": 3, \"width\": 968, \"height\": 537}]"
motivation: 现有多模态讽刺检测方法未考虑不同模态可靠性差异，导致融合过程易受噪声干扰。
method: 采用多头跨注意力与自注意力结合的融合机制，并基于高斯后验建模各模态不确定性以动态调节融合。
result: 在公开数据集上，URMF性能优于多种强基线模型，在准确率与鲁棒性上有显著提升。
conclusion: URMF验证了不确定性建模能有效提升多模态讽刺检测的鲁棒性。
---

## 摘要
多模态讽刺检测（Multimodal Sarcasm Detection, MSD）旨在通过文本与图像之间的语义不一致来识别讽刺意图。尽管近期的方法通过跨模态交互与不一致性推理提升了 MSD 的效果，但它们通常假设所有模态同样可靠。然而在现实的社交媒体场景中，文本内容可能含糊不清，视觉内容可能弱相关甚至无关，从而使确定性融合引入噪声证据并削弱鲁棒推理。为解决这一问题，我们提出了不确定性感知的鲁棒多模态融合框架（Uncertainty-aware Robust Multimodal Fusion, URMF），该统一框架在交互与融合过程中显式建模模态的可靠性。URMF 首先采用多头跨注意力机制将视觉证据注入文本表示中，随后在融合的语义空间中通过多头自注意力强化不一致性推理。接着，URMF 对文本、图像以及交互感知的潜在表示进行统一的单模态随机不确定性建模，将每种模态参数化为可学习的高斯后验。估计出的不确定性进一步用于在融合过程中动态调节各模态贡献，抑制不可靠模态，从而获得更鲁棒的联合表示。此外，我们设计了一个联合训练目标，融合了任务监督、模态先验正则化、跨模态分布对齐及基于不确定性的自采样对比学习。在公开的 MSD 基准上进行的实验表明，URMF 持续优于强大的单模态、多模态以及跨大型语言模型（MLLM）基线，验证了不确定性感知融合在提升准确性与鲁棒性方面的有效性。

## Abstract
Multimodal sarcasm detection (MSD) aims to identify sarcastic intent from semantic incongruity between text and image. Although recent methods have improved MSD through cross-modal interaction and incongruity reasoning, they often assume that all modalities are equally reliable. In real-world social media, however, textual content may be ambiguous and visual content may be weakly relevant or even irrelevant, causing deterministic fusion to introduce noisy evidence and weaken robust reasoning. To address this issue, we propose Uncertainty-aware Robust Multimodal Fusion (URMF), a unified framework that explicitly models modality reliability during interaction and fusion. URMF first employs multi-head cross-attention to inject visual evidence into textual representations, followed by multi-head self-attention in the fused semantic space to enhance incongruity-aware reasoning. It then performs unified unimodal aleatoric uncertainty modeling over text, image, and interaction-aware latent representations by parameterizing each modality as a learnable Gaussian posterior. The estimated uncertainty is further used to dynamically regulate modality contributions during fusion, suppressing unreliable modalities and yielding a more robust joint representation. In addition, we design a joint training objective integrating task supervision, modality prior regularization, cross-modal distribution alignment, and uncertainty-driven self-sampling contrastive learning. Experiments on public MSD benchmarks show that URMF consistently outperforms strong unimodal, multimodal, and MLLM-based baselines, demonstrating the effectiveness of uncertainty-aware fusion for improving both accuracy and robustness.

---

## 论文详细总结（自动生成）

# URMF：不确定性感知的鲁棒多模态融合用于多模态讽刺检测 —— 中文总结

---

## 一、研究问题与动机

- **研究背景**：  
  多模态讽刺检测（Multimodal Sarcasm Detection, MSD）旨在通过分析图像与文本间的**语义不一致**来识别讽刺意图。在社交媒体环境中，讽刺常通过言外之意与情感反转表现，因此需理解文字和图像间的冲突。

- **问题与动机**：  
  现有MSD方法多假定各模态信息**同样可靠、噪声一致**，进行确定性融合。然而：
  - 文本可能存在歧义、缺乏上下文；
  - 图像可能仅作装饰、不相关甚至矛盾；
  - 这使得直接融合会掺入噪声、降低鲁棒性。  
  因此，作者提出需要在**融合过程中显式建模模态的不确定性与可靠性**，以实现更鲁棒的跨模态讽刺推理。

---

## 二、方法论：URMF 框架设计

### 1. 核心思想
> **显式建模模态不确定性（aleatoric uncertainty）**，通过学习每个模态的置信度，动态调整其在融合中的权重，从而过滤低可靠模态的干扰。

### 2. 整体流程
URMF（Uncertainty-aware Robust Multimodal Fusion）包括四大阶段：

1. **跨模态交互（Cross-modal Interaction）**  
   - 使用单层 Transformer 结构，采用“**先跨模态对齐，后内部推理**”的顺序；
   - 多头**跨注意力（MHCA）**将图像语义注入文本表示；
   - 多头**自注意力（MHSA）**在融合空间建模令牌依赖；
   - 得到融合的潜在表示 \( X_f \)。

2. **单模态随机不确定性建模（Aleatoric Uncertainty Modeling）**  
   - 对文本（t）、图像（i）和交互潜变量（f）分别学习均值 μ 和方差 σ：
     \[
     p(z_{km}|x_{km}) = \mathcal{N}(\mu_{km}, \sigma_{km}^2 I)
     \]
   - 通过重参数化采样 \( z_{km} = \mu_{km} + \sigma_{km}\epsilon \)。

3. **不确定性引导的动态融合（Dynamic Fusion）**  
   - 将每个模态的不确定性综合为标量：
     \[
     \bar{\sigma}_m^2 = \frac{1}{D} \sum_d \sigma_{m,d}^2
     \]
   - 低不确定性对应高权重：
     \[
     \alpha_m = \frac{\exp(\frac{1}{\bar{\sigma}_m^2 + \epsilon})}{\sum_s \exp(\frac{1}{\bar{\sigma}_s^2 + \epsilon})}
     \]
   - 最终多模态表示 \( h = \phi([\alpha_f \mu_f, \alpha_i \mu_i]) \)。

4. **联合训练目标（Joint Objective）**  
   结合四种互补损失项：
   - **信息瓶颈约束（LIB）**：确保紧凑且任务充分的表示；
   - **模态正则化（Lreg）**：平衡各模态分布；
   - **跨模态分布对齐（Lalign）**：减少模态间统计移位；
   - **不确定性驱动对比学习（LUCL）**：利用采样噪声构建对比视图增强鲁棒性。  
   总损失：
   \[
   L = L_{IB} + \lambda_1 L_{reg} + \lambda_2 L_{align} + \lambda_3 L_{UCL}
   \]

---

## 三、实验设计

### 1. 数据集与评估指标
- 使用公共 **Multimodal Sarcasm Detection (MSD)** 数据集。  
- 评估指标：Accuracy、Precision、Recall、F1-Score。

### 2. 对比方法
- **单模态基线**：ResNet、ViT（图像）；Bi-LSTM、SIARN、SMSD、BERT、RoBERTa（文本）。  
- **多模态基线**：HFM、CMGCN、MuMu、MICL、DynRT-Net、CIRM、SCI-GDFN 等20余种方法。  
- **大型多模态语言模型（MLLM）基线**：LLaVA1.5、LLaVA1.5-VIDR。

### 3. 实验配置与硬件资源
- 文中明确指出使用**单块 NVIDIA GeForce RTX 4090 GPU**，采用 RoBERTa 作为文本编码器，ViT 作为图像编码器。  
- 论文未说明训练时长或具体算力消耗。

---

## 四、实验数量与充分性

- **主要实验**：在 MSD 数据集上进行完整性能对比。  
- **消融实验**：6 组消融，包括去除各损失项（LIB、Lreg、Lalign、LUCL）、关闭动态融合以及替换标准交互顺序（标准 Transformer）。  
- **定性分析**：t-SNE 可视化比较不同变体的潜在空间分布。  
- **充分性与客观性**：覆盖任务主流对比、包含多层 ablation，并以统一预训练编码器为基线，评估标准公平合理。

---

## 五、主要结果与发现

- **整体表现**：  
  URMF 在 MSD 上取得 **Accuracy = 95.02%**，**F1 = 94.91%**，超越所有对比模型。  
- 相较强基线 **DynRT-Net** 提升约 **+1.6% F1**；
- **优于 MLLM（LLaVA1.5）**，说明专用不确定性建模优于通用大模型适配；
- 消融结果显示：每个关键组件缺失均会导致性能下降，动态融合与交互顺序影响最大；
- 表示可视化显示 URMF 形成更清晰的类间分隔与紧凑的类内分布，说明鲁棒性增强。

---

## 六、研究结论与发现

- 显式模态不确定性建模显著提升多模态讽刺检测的**鲁棒性与泛化性**；
- 基于不确定性的权重自适应机制实现了针对性融合；
- 结合对比学习与分布正则使模型在噪声数据下保持稳定；
- 证实“**不确定性信号既可衡量模态可靠性，也能作为训练过程的正则引导因素**”。

---

## 七、优点与创新点

- **方法层面创新**  
  - 首次将**aleatoric uncertainty**引入MSD任务；
  - 提出“**不确定性驱动的动态自适应融合**”机制；
  - 设计统一训练目标，融合信息瓶颈、正则、分布对齐与对比学习。

- **实验设计**  
  - 广泛对比近二十种强基线并包含MLLM；
  - 消融实验详尽，结合定量与可视化分析。

- **性能表现**  
  - 在公开基准上实现最新SOTA，验证了跨模态鲁棒性改进。

---

## 八、不足与局限

- **实验范围**：仅在单一 MSD 数据集上验证，缺乏跨领域/多语言测试；  
- **算力说明不完整**：未提供训练时间、批次大小、参数规模等细节；  
- **开放适用性**：虽然优于 MLLM，但与更大规模多模态基础模型的泛化测试尚未呈现；  
- **潜在风险**：高斯假设下的不确定性建模在复杂多峰分布中可能不充分，限制对复杂模态的建模能力。

---

**（完）**
