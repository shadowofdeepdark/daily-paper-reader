---
title: "URMF: Uncertainty-aware Robust Multimodal Fusion for Multimodal Sarcasm Detection"
title_zh: URMF：用于多模态讽刺检测的不确定性感知稳健多模态融合
authors: "Zhenyu Wang, Weichen Cheng, Weijia Li, Junjie Mou, Zongyou Zhao, Guoying Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06728v1"
tags: ["query:q2"]
score: 9.0
evidence: 多模态讽刺检测，通过文本与图像间的语义不一致识别意图
tldr: 本文提出URMF框架，通过显式建模模态不确定性，解决多模态讽刺检测中模态可靠性差异导致的融合噪声问题。方法利用不确定性感知机制动态调节各模态贡献，实现稳健且准确的讽刺识别，在多个基准上取得优异结果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06728v1/fig-001.webp\", \"caption\": \"Figure 2: t-SNE visualization of joint latent representations on the MSD test set for major ablation variants and the full URMF.\", \"page\": 9, \"index\": 1, \"width\": 976, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.06728v1/fig-002.webp\", \"caption\": \"Table 2: Main results on the MSD dataset. Best results are shown in bold and second-best results are underlined.\", \"page\": 8, \"index\": 2, \"width\": 772, \"height\": 1029}, {\"url\": \"assets/figures/arxiv/2604.06728v1/fig-003.webp\", \"caption\": \"Figure 1: Given an image-text pair (I, T ), URMF performs cross-modal interaction, modality uncertainty modeling, and uncertainty-guided dynamic fusion to learn a joint latent representation for sarcasm prediction. The model is trained end-to-end with information bottleneck, modality regularization, multimodal alignment, and uncertainty contrastive learning losses.\", \"page\": 4, \"index\": 3, \"width\": 968, \"height\": 537}]"
motivation: 现有多模态讽刺检测模型假设各模态同等可靠，现实中模态噪声会削弱融合效果。
method: 采用多头交互注意力和自注意力融合语义表示，并以高斯后验形式建模各模态不确定性进行动态权重调整。
result: 在公开的多模态讽刺检测基准上，URMF显著优于单模态、多模态及大型多模态语言模型基线。
conclusion: 不确定性感知的多模态融合显著提升讽刺检测的准确性与鲁棒性。
---

## 摘要
多模态讽刺检测（Multimodal Sarcasm Detection，MSD）旨在识别文本与图像之间语义不一致所表达的讽刺意图。尽管近年来的方法通过跨模态交互与不一致性推理提升了MSD的性能，但它们通常假设所有模态同等可靠。然而，在真实的社交媒体环境中，文本内容可能存在歧义，而视觉内容可能仅弱相关甚至完全无关，从而导致确定性融合引入噪声证据，削弱稳健的推理能力。为解决这一问题，我们提出了不确定性感知的稳健多模态融合框架（Uncertainty-aware Robust Multimodal Fusion，URMF），该统一框架在交互与融合过程中显式建模模态可靠性。URMF首先采用多头跨注意力机制将视觉证据注入文本表示中，随后在融合的语义空间中通过多头自注意力机制增强不一致性感知的推理。接着，它通过将每种模态参数化为可学习的高斯后验分布，对文本、图像及交互感知的潜在表示进行统一的单模态随机不确定性（aleatoric uncertainty）建模。估计出的不确定性进一步用于在融合过程中动态调节模态贡献，抑制不可靠的模态，从而获得更稳健的联合表示。此外，我们设计了一个联合训练目标，综合了任务监督、模态先验正则化、跨模态分布对齐与基于不确定性的自采样对比学习。在公共MSD基准数据集上的实验表明，URMF在强单模态、多模态及基于MLLM的基线模型上均实现了一致超越，验证了不确定性感知融合在提升准确性与鲁棒性方面的有效性。

## Abstract
Multimodal sarcasm detection (MSD) aims to identify sarcastic intent from semantic incongruity between text and image. Although recent methods have improved MSD through cross-modal interaction and incongruity reasoning, they often assume that all modalities are equally reliable. In real-world social media, however, textual content may be ambiguous and visual content may be weakly relevant or even irrelevant, causing deterministic fusion to introduce noisy evidence and weaken robust reasoning. To address this issue, we propose Uncertainty-aware Robust Multimodal Fusion (URMF), a unified framework that explicitly models modality reliability during interaction and fusion. URMF first employs multi-head cross-attention to inject visual evidence into textual representations, followed by multi-head self-attention in the fused semantic space to enhance incongruity-aware reasoning. It then performs unified unimodal aleatoric uncertainty modeling over text, image, and interaction-aware latent representations by parameterizing each modality as a learnable Gaussian posterior. The estimated uncertainty is further used to dynamically regulate modality contributions during fusion, suppressing unreliable modalities and yielding a more robust joint representation. In addition, we design a joint training objective integrating task supervision, modality prior regularization, cross-modal distribution alignment, and uncertainty-driven self-sampling contrastive learning. Experiments on public MSD benchmarks show that URMF consistently outperforms strong unimodal, multimodal, and MLLM-based baselines, demonstrating the effectiveness of uncertainty-aware fusion for improving both accuracy and robustness.

---

## 论文详细总结（自动生成）

# URMF：用于多模态讽刺检测的不确定性感知稳健多模态融合  
——论文结构化中文总结  

---

## 一、核心问题与研究背景

- **研究任务**：多模态讽刺检测（Multimodal Sarcasm Detection, MSD）旨在识别社交媒体中图文之间语义不一致所表达的讽刺意图。  
- **问题背景**：典型讽刺文本常通过文字与图片的语义冲突来表达隐含态度或情绪。现有模型虽在跨模态交互与不一致性推理方面取得进展，但大多数都**隐含假设各模态同等可靠**。  
- **现实困境**：在真实社交媒体场景中：
  - 文本语义可能含糊、隐喻、缺乏上下文；
  - 图像可能仅起装饰作用或与讽刺内容弱相关甚至无关。  
  因此，传统确定性融合容易将噪声模态纳入决策，**稀释关键冲突线索，降低鲁棒性**。  
- **研究动机**：作者提出需显式建模模态可靠性，引入**不确定性估计（uncertainty modeling）**作为稳健多模态学习的机制，从根本提升讽刺检测的准确性与稳定性。

---

## 二、方法论设计

### 1. 核心思想
提出 **URMF（Uncertainty-aware Robust Multimodal Fusion）** 框架，通过**显式建模文本、图像及交互表示的不确定性（aleatoric uncertainty）**，实现动态图文融合，从而提升在噪声或弱相关模态下的鲁棒性。

### 2. 技术结构与流程
URMF包含四个主要阶段：

1. **跨模态交互（Cross-modal Interaction）**
   - 采用单层Transformer结构，先执行**多头跨注意力（MHCA）**：将视觉信息注入文本表示；
   - 再通过**多头自注意力（MHSA）**在融合的语义空间中进行推理，以强化语义冲突建模；
   - 输出全局交互表示 `Xf = Pool(X'_t)`。

2. **单模态不确定性建模（Unimodal Aleatoric Uncertainty）**
   - 对文本、图像和交互模态的表示分别建立高斯后验分布：  
     \( p(z_m|x_m) = N(\mu_m, \sigma_m^2 I) \)；
   - 均值 μ 表示语义中心，方差 σ 表征模态噪声；
   - 通过重参数化（reparameterization）采样：  
     \( z_m = \mu_m + \sigma_m \epsilon, \ \epsilon \sim N(0,I) \)。

3. **不确定性引导的动态融合（Uncertainty-guided Dynamic Fusion）**
   - 根据各模态的平均方差 \( \bar{\sigma}_m^2 \) 计算权重：  
     \( a_m = \exp(1/(\bar{\sigma}_m^2 + \epsilon)) \)，再归一化得到 \(\alpha_m\)；
   - 低不确定性模态权重更高，融合后的表示：  
     \( \hat{x}_m = \alpha_m \mu_m \)；
   - 最终联合网络 \( h = \phi([\hat{x}_f, \hat{x}_i]) \) 得到 joint 表示，再建模为高斯后验 \( p(z_h|h) \)，用于分类预测。

4. **联合训练目标（Joint Optimization Objective）**
   - 总损失：  
     \( L = L_{IB} + \lambda_1 L_{reg} + \lambda_2 L_{align} + \lambda_3 L_{UCL} \)，包含四部分：
     - **信息瓶颈约束 (LIB)**：用交叉熵 + KL 正则，保证表示紧致且任务充分；
     - **模态先验正则 (Lreg)**：对各模态分布向标准高斯逼近；
     - **跨模态分布对齐 (Lalign)**：缩小文本、交互与图像模态的分布差距；
     - **基于不确定性的自采样对比学习 (LUCL)**：利用随机采样生成正/负对比样本，提高噪声鲁棒性。

总体上，URMF通过**“跨模态交互 + 分布式不确定性建模 + 动态权重融合 + 联合正则训练”**构成完整的鲁棒多模态学习流程。

---

## 三、实验设计

- **数据集**：使用公开的 **Multimodal Sarcasm Detection (MSD)** 数据集，遵循官方训练/验证/测试划分。  
- **评估指标**：Accuracy、Precision、Recall、F1-score。  
- **对比模型**：
  - **单模态基线**：ResNet, ViT（图像）；Bi-LSTM, SIARN, SMSD, BERT, RoBERTa（文本）。  
  - **多模态基线**：涵盖 HFM, CMGCN, DIP, MuMu, MICL, DynRT-Net, AMIF, CIRM, SCI-GDFN 等近二十个代表模型。  
  - **MLLM 基线**：包括 LLaVA1.5 和 LLaVA1.5-VIDR，大型多模态语言模型的微调版本。
- **实验结果**：
  - URMF 在 MSD 上取得最高成绩：
    - Accuracy 95.02%，Precision 94.69%，Recall 95.19%，F1-score 94.91。
  - 超越所有传统与 MLLM 基线。

---

## 四、资源与算力

- **硬件环境**：文中明确说明实验在 **NVIDIA GeForce RTX 4090 单卡**上运行。  
- **算力说明**：未给出训练时长或批次大小等具体参数，仅说明“单 GPU 训练、端到端优化”。  
- **计算量推测**：模型包含Transformer交互与高斯后验计算，计算量中等偏高，但文章未提供 FLOPs 或训练耗时。

---

## 五、实验数量与充分性

- **主要实验**：
  - 性能主实验（Table 2）：对比近 30 个单模态与多模态模型；
  - **消融实验（Table 3）**：移除六个核心组件（LIB、Lreg、Lalign、LUCL、动态融合、交互顺序），分别分析性能下降；
  - **可视化实验（Figure 2）**：对比主要变体的 t-SNE 分布图。  
- **充分性**：  
  - 实验覆盖面广，包括跨模态、单模态、MLLM 三类基线；
  - 消融设计细致，性能下降显著且可解释；
  - 数据单一（MSD），但为权威基准，结果可信；  
  - 未跨数据集验证，一定程度限制泛化性评估。

---

## 六、主要结论与发现

- **URMF 显式建模模态不确定性可有效提升鲁棒性**；
- **动态融合机制能自动抑制高噪声模态**，保持关键冲突线索；
- **对比学习结合采样不确定性增加表示多样性**；
- 相较大型预训练模型（LLaVA系）亦显著优越，表明**任务特定的结构化不确定性建模优于通用大模型微调**；
- **定量与定性结果一致性高**：t-SNE显示类别分布更紧凑、界限更清晰。

---

## 七、优点与创新点

- **概念创新**：将**aleatoric uncertainty**引入多模态讽刺检测，突破以往“同质可靠性”假设。  
- **设计创新**：
  - 独特的交互顺序：“先跨模态，后自注意”优化了图文冲突建模；
  - 全流程联合正则（IB + 对齐 + 对比），构成稳健训练体系。  
- **实验完整、优势突出**：性能刷新 MSD benchmark；同时兼顾准确率与鲁棒性。  
- **推理机制稳健可解释**：通过不确定性分布和动态权重直观解释模态影响。

---

## 八、不足与局限

- **数据局限**：仅在单一 MSD 数据集评估，缺乏跨领域、跨语言验证。  
- **算力与规模未说明**：训练时长、参数量等未披露，难以复现实验规模。  
- **模型复杂度较高**：包含多层分布建模和对比采样，计算成本可能较大。  
- **泛化性与实时性待验证**：在真实社交媒体的动态环境下仍需进一步测试。  
- **对 epistemic uncertainty 未涉及**：仅关注输出噪声（aleatoric），尚未结合模型不确定性层面。

---

（完）
