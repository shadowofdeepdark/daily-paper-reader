---
title: "URMF: Uncertainty-aware Robust Multimodal Fusion for Multimodal Sarcasm Detection"
title_zh: URMF：一种面向多模态讽刺检测的不确定性感知鲁棒多模态融合方法
authors: "Zhenyu Wang, Weichen Cheng, Weijia Li, Junjie Mou, Zongyou Zhao, Guoying Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06728v1"
tags: ["query:q2"]
score: 9.0
evidence: 多模态讽刺检测以及文本与图像间的语义不一致性
tldr: 本文提出一种名为URMF的不确定性感知多模态融合框架，用于讽刺检测。该方法同时建模文本和图像模态的不确定性，通过多头交叉注意与自注意增强语义不一致推理，并根据估计的不确定性自适应调节各模态贡献，从而实现更稳健、准确的讽刺识别。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06728v1/fig-001.webp\", \"caption\": \"Figure 2: t-SNE visualization of joint latent representations on the MSD test set for major ablation variants and the full URMF.\", \"page\": 9, \"index\": 1, \"width\": 976, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.06728v1/fig-002.webp\", \"caption\": \"Table 2: Main results on the MSD dataset. Best results are shown in bold and second-best results are underlined.\", \"page\": 8, \"index\": 2, \"width\": 772, \"height\": 1029}, {\"url\": \"assets/figures/arxiv/2604.06728v1/fig-003.webp\", \"caption\": \"Figure 1: Given an image-text pair (I, T ), URMF performs cross-modal interaction, modality uncertainty modeling, and uncertainty-guided dynamic fusion to learn a joint latent representation for sarcasm prediction. The model is trained end-to-end with information bottleneck, modality regularization, multimodal alignment, and uncertainty contrastive learning losses.\", \"page\": 4, \"index\": 3, \"width\": 968, \"height\": 537}]"
motivation: 现有多模态讽刺检测方法忽视了模态可靠性的差异，导致融合结果易受噪声影响。
method: 该方法通过多头注意机制交互特征，并以可学习高斯后验建模各模态不确定性，用其动态调节融合权重。
result: 在公开数据集上，URMF在准确性和鲁棒性上显著优于多模态和大型语言模型基线。
conclusion: URMF验证了在融合过程中显式建模模态不确定性能有效提升讽刺检测的性能与鲁棒性。
---

## 摘要
多模态讽刺检测（Multimodal Sarcasm Detection, MSD）的目标是从文本与图像之间的语义不一致中识别讽刺意图。尽管近年来的方法通过跨模态交互与不一致性推理提升了MSD的性能，但它们往往假设所有模态同样可靠。实际上，在社交媒体场景中，文本内容可能存在歧义，而视觉内容可能关联性较弱甚至无关，从而使确定性融合引入噪声证据，削弱鲁棒推理能力。针对这一问题，我们提出了一种不确定性感知的鲁棒多模态融合框架（Uncertainty-aware Robust Multimodal Fusion, URMF），该统一框架在交互与融合过程中显式建模模态可靠性。URMF首先采用多头交叉注意力机制将视觉证据注入文本表示中，随后在融合的语义空间中应用多头自注意力以增强对不一致性的推理。接着，它在文本、图像及交互感知的潜在表示上执行统一的单模态随机不确定性（aleatoric uncertainty）建模，通过将每种模态参数化为可学习的高斯后验来实现。估计得到的不确定性进一步用于动态调节模态在融合过程中的贡献，从而抑制不可靠模态并生成更鲁棒的联合表示。此外，我们设计了联合训练目标，综合了任务监督、模态先验正则化、跨模态分布对齐以及基于不确定性驱动的自采样对比学习。在公开的MSD基准上进行的实验表明，URMF在单模态、多模态和基于MLLM的强基线上均取得了持续领先的表现，验证了不确定性感知融合在提升准确性与鲁棒性方面的有效性。

## Abstract
Multimodal sarcasm detection (MSD) aims to identify sarcastic intent from semantic incongruity between text and image. Although recent methods have improved MSD through cross-modal interaction and incongruity reasoning, they often assume that all modalities are equally reliable. In real-world social media, however, textual content may be ambiguous and visual content may be weakly relevant or even irrelevant, causing deterministic fusion to introduce noisy evidence and weaken robust reasoning. To address this issue, we propose Uncertainty-aware Robust Multimodal Fusion (URMF), a unified framework that explicitly models modality reliability during interaction and fusion. URMF first employs multi-head cross-attention to inject visual evidence into textual representations, followed by multi-head self-attention in the fused semantic space to enhance incongruity-aware reasoning. It then performs unified unimodal aleatoric uncertainty modeling over text, image, and interaction-aware latent representations by parameterizing each modality as a learnable Gaussian posterior. The estimated uncertainty is further used to dynamically regulate modality contributions during fusion, suppressing unreliable modalities and yielding a more robust joint representation. In addition, we design a joint training objective integrating task supervision, modality prior regularization, cross-modal distribution alignment, and uncertainty-driven self-sampling contrastive learning. Experiments on public MSD benchmarks show that URMF consistently outperforms strong unimodal, multimodal, and MLLM-based baselines, demonstrating the effectiveness of uncertainty-aware fusion for improving both accuracy and robustness.

---

## 论文详细总结（自动生成）

# URMF：不确定性感知鲁棒多模态融合用于多模态讽刺检测 — 中文详细总结

---

## 一、研究背景与核心问题

### 研究动机
- **任务定义**：多模态讽刺检测（Multimodal Sarcasm Detection, MSD）旨在识别文本与图像间的语义不一致或冲突，以判断是否存在讽刺意图。  
- **现有问题**：多数现有模型假设不同模态（文本与图像）输入**同等可靠且确定性强**；然而在真实社交媒体语境中，该假设常被破坏：
  - 文本可能含糊、修辞强或上下文缺失；
  - 图像可能仅为装饰或与讽刺意图弱相关。
- **后果**：传统的确定性融合容易引入噪声证据、扩散关键冲突特征、削弱鲁棒性和泛化性能。
- **研究目标**：提出一种能**识别并适应不同模态可靠性**的鲁棒融合框架，以提升在噪声或不匹配条件下的讽刺检测性能。

---

## 二、方法论：URMF 框架

### 核心思想
> 在跨模态交互与融合过程中，显式建模各模态的不确定性，并利用该不确定性动态调节模态贡献，从而构建更鲁棒的融合表示。

### 模型总体流程
1. **跨模态交互阶段（Cross-modal Interaction）**
   - 采用单层 Transformer 架构，信息流顺序为：
     - **先交叉注意力（Cross-Attention）再自注意力（Self-Attention）**。
   - 图像特征通过多头交叉注意力注入文本表示，随后自注意在融合空间中强化语义不一致推理。

2. **单模态不确定性建模**
   - 对三种模态（文本 t、图像 i、交互感知模态 f）分别进行**Aleatoric 不确定性建模**。
   - 每个模态表示被参数化为可学习的**高斯分布后验**：
     - 均值向量代表语义中心；
     - 方差向量刻画观测的不确定性。
   - 通过重参数化采样实现随机潜变量生成：
     \[
     z_m = \mu_m + \sigma_m \epsilon, \quad \epsilon \sim N(0, I)
     \]

3. **不确定性引导的动态融合**
   - 根据各模态的不确定性大小（平均方差）计算权重：
     \[
     \alpha_m = \frac{\exp(1 / (\bar{\sigma}_m^2 + \epsilon))}{\sum_{s}\exp(1 / (\bar{\sigma}_s^2 + \epsilon))}
     \]
   - 低不确定性模态贡献较大，高不确定性模态被自适应抑制。
   - 组合加权表示后输入联合网络并再次进行高斯建模形成最终联合潜在向量。

4. **联合训练目标**
   - 总损失：
     \[
     L = L_{IB} + \lambda_1 L_{reg} + \lambda_2 L_{align} + \lambda_3 L_{UCL}
     \]
   - 其中包含：
     - **信息瓶颈损失（LIB）**：确保任务相关但紧凑的表示。
     - **模态正则化（Lreg）**：约束单模态分布向标准高斯靠拢。
     - **跨模态分布对齐（Lalign）**：通过 KL 散度协调不同模态潜在空间。
     - **不确定性感知对比学习（LUCL）**：以采样噪声形成正负对比视图，提高鲁棒性。

---

## 三、实验设计

### 数据集与评价指标
- 使用公开 **Multimodal Sarcasm Detection (MSD)** 数据集。
- 采用官方训练/验证/测试划分。
- 评估指标：**Accuracy、Precision、Recall、F1-score**。

### 对比基线
- **单模态模型**：
  - 图像：ResNet、ViT；
  - 文本：Bi-LSTM、BERT、RoBERTa 等。
- **多模态模型**：
  - 包含 HFM、D&R Net、Bridge、CMGCN、MICL、DynRT-Net、CIRM、SCI-GDFN 等二十余种最新方法。
- **MLLM 模型**：
  - LLaVA1.5 及其改进版 LLaVA1.5-VIDR。

### 模型实现细节
- 文本编码器采用 **RoBERTa**；图像编码器采用 **ViT**。
- 超参数如 KL 权重 λ_IB=10^-3，融合正则 λ_1=10^-3，分布对齐 λ_2=10^-5，对比学习 λ_3=10^-3。

---

## 四、资源与算力

- 作者指出所有实验均在 **单张 NVIDIA GeForce RTX 4090 GPU** 上进行。  
- 未明确说明训练时长或批次规模。  
- 可视为**中等规模计算资源**实验。

---

## 五、实验数量与充分性

- **主实验**：在 MSD 基准进行完整对比，涵盖三类模型（单模态、多模态、MLLM）。
- **消融实验**：
  - 共 6 个变体：分别移除 LIB、Lreg、Lalign、LUCL、Dynamic Fusion 或替换为标准 Transformer。
  - 每个变体均重复完整评估。
- **定性分析**：通过 t-SNE 可视化分析不同变体的潜在空间分布。
- **充分性评价**：
  - 覆盖了主要模块，实验对照清晰；
  - 客观性较高（基线丰富、指标全面）；
  - 数据仅限 MSD，跨数据集验证仍待补充。

---

## 六、主要结论与发现

1. **性能表现**：
   - URMF 在 MSD 上达到最高性能：  
     - Accuracy 95.02%、Precision 94.69%、Recall 95.19%、F1 94.91%，均超越所有基线。
2. **核心发现**：
   - 显式建模单模态不确定性并将其用于动态融合，可有效：
     - 抑制不可靠模态；
     - 提升鲁棒性；
     - 保留讽刺相关冲突线索。
   - 对比 MLLM（如 LLaVA1.5）仍保持优势，显示任务专向设计优于通用模型。

---

## 七、优点与创新

- **方法创新性强**：首次将单模态 aleatoric 不确定性显式引入多模态讽刺检测。
- **架构设计合理**：
  - 独特的“先交叉后自注意”交互顺序，有助捕捉语义冲突。
- **融合机制自适应**：
  - 动态权重根据不确定性调整，改善了对噪声与弱关联输入的鲁棒性。
- **训练目标综合化**：
  - 同时结合信息瓶颈、模态正则化、跨模态对齐与不确定性驱动对比学习，形成完整优化闭环。
- **实验验证扎实**：
  - 超越多类基线与大型模型，体现方法的普适有效性。

---

## 八、不足与局限

- **数据覆盖有限**：仅在 MSD 单一基准测试，缺乏跨数据集（如 MMSD3.0）或多语言验证。
- **计算资源描述不充分**：未给出训练时长与参数量，无法评估可扩展性。
- **模型复杂性高**：多层不确定性建模与对比学习损失可能增加计算开销。
- **适用性受限**：主要面向讽刺检测任务，对其他多模态理解任务的泛化能力尚待验证。
- **未探讨开源复现**：框架的复现性信息不足（未说明代码或数据可用性）。

---

**（完）**
